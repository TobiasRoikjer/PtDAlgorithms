#ifndef PTDALGORITHMS_GENERATION_H
#define PTDALGORITHMS_GENERATION_H

#include "phase.h"

vertex_t *generate_graph(unsigned int seed, size_t n_states, size_t n_edges, size_t n_zero_rewards);

#endif //PTDALGORITHMS_GENERATION_H
